apiVersion: v1
kind: Service
metadata:
  name: {{ include "hebbs.fullname" . }}
  labels:
    {{- include "hebbs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "hebbs.selectorLabels" . | nindent 4 }}
